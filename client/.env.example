# Backend API base URL (no trailing slash)
# Local dev: leave empty — Vite proxies /api → http://localhost:5000
VITE_API_URL=

# Production (client hosted separately from API only):
# VITE_API_URL=https://your-api.onrender.com
